/**
 * Bump this whenever the catalog is regenerated (`npm run catalog`) or the
 * product data is otherwise edited, so the "last updated" line on the
 * homepage stays honest.
 */
export const catalogUpdatedAt = '2026-09-20';
